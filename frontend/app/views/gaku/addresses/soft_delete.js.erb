showNotice("<%= render_flash %>");

$("#<%= @polymorphic_resource_name.pluralize %>-tab-link").html("<%= print_count(@count, t_address_plural) %>");
$(".<%= @polymorphic_resource_name.pluralize %>-count").html("<%= print_count(@count, t_address_list) %>");

<%- if @polymorphic_resource.addresses.reload.any? & @primary_address %>
	$('#address-<%= @polymorphic_resource.addresses.first.id %> td a').addClass('btn-primary');
<% end %>