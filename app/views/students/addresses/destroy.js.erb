$('#new-student-address-tab-link').html('<%= print_count(@student.addresses.count, t('addresses.addresses')) %>');
$('.student-addresses-count').html('<%= print_count(@student.addresses.count, t('addresses.list')) %>');
showNotice("<%= render_flash %>");

<%- unless @student.addresses.blank? %>
	$('#address-<%= @student.addresses.first.id %> td a').addClass('btn-primary');
<% end %>
