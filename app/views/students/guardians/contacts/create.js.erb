	$('.guardian_contact_table tbody').append('<%= escape_javascript(render :partial => "contact", :formats => [:html], :handlers => [:erb, :slim], :locals => {:contact => @contact}) %>');
$("#new_guardian_contact_link").show();
$('#new_guardian_contact_form').hide();
$('#new_guardian_contact_form form')[0].reset();

<% if @contact.is_primary? %>
	$('.guardian_contact').each(function() {
		$(this).find('.make_primary').removeClass('btn-primary')
	});
	var currentContact = $("#contact_<%= @contact.id %>");
	currentContact.find('.make_primary').addClass('btn-primary');
<% end %>	