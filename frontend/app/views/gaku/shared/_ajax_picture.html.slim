= image_tag resource.picture.url(:thumb), id: "avatar-picture", class: "img-thumbnail"

- if can_edit? or action_name == 'set_picture'
  .row.mt-m
    .col-md-12
      = link_to_upload_image 'JavaScript:void(0);', method: :get, id: "upload-picture-link"
      #upload-picture.d-none
        = render 'gaku/shared/ajax_upload_picture', resource: resource
