$('#admin-admission-method-admission-phases-index tbody').append('<%= render_js_partial("admission_phase", {:admission_phase => @admission_phase}) %>');

$('#new-admin-admission-method-admission-phase').slide();
$('#new-admin-admission-method-admission-phase-link').show();

showNotice("<%= render_flash %>");
$('.admin-admission-method-admission-phases-count').html('<%= print_count(@admission_phases_count + 1, t("admission_phases.list")) %>');
