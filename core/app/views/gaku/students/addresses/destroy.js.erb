$("#student-addresses-tab-link").html("<%= print_count(@student.addresses.count, t('address.plural')) %>");
$(".student-addresses-count").html("<%= print_count(@student.addresses.count, t('address.list')) %>");
showNotice("<%= render_flash %>");

<%- unless @student.addresses.blank? %>
	$('#address-<%= @student.addresses.first.id %> td a').addClass('btn-primary');
<% end %>
