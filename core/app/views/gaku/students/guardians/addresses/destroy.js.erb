<%- unless @guardian.addresses.blank? %>
	$('#address-<%= @guardian.addresses.first.id%> td a').addClass('btn-primary');
<% end %>

$('.student-guardian-addresses-count').html('<%= print_count(@guardian.addresses.count, t('address.list')) %>');
showNotice("<%= render_flash %>");
