$('#course-modal').modal('hide');
$('#course-modal').remove();
$('#notice').html("<%= escape_javascript(render 'shared/flash', :flash => flash) %>");

var currentCourseSyllabus= $(".course-<%= @course.id %> .syllabus");
currentCourseSyllabus.html( "[<%= @course.syllabus.code + "]:" +  @course.syllabus.name + "-" + @course.code %>");  