$('#student-course-enrollments-tab-link').html("<%= print_count(@student.courses.count, t('course.plural'))  %>");
$('.student-course-enrollments-count').html("<%= print_count(@student.courses.count, t('course.list')) %>");
showNotice("<%= render_flash %>");
