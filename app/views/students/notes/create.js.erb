$('#student_notes table tbody').append('<%= escape_javascript(render :partial => "students/notes/show", :formats => [:html], :handlers => [:erb, :slim], :locals => {:guardian => @student.guardians.last}) %>');
$('.notes_count').html('<%= @student.notes.count %>');
