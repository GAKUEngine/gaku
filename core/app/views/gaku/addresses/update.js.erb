showNotice("<%= render_flash %>");

$('tr#address-<%= @address.id %>').html('<%= j render("fields", { address: @address, target: prepare_target([
  @nested_resources, @poymorphic_resource], @address) }) %>');
$(".modal#edit-address-modal").modal("hide");