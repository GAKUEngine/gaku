$('#new-student-note-tab-link').html('<%= @student.notes.count != 0 ? t('notes.notes') + "（" + @student.notes.count.to_s + "）" : t('notes.notes') %>');
$('.student-notes-count').html('<%= @student.notes.count != 0 ? t('notes.list') + "（" + @student.notes.count.to_s + "）" : t('notes.list') %>');
