<%= form_for(@class_group_enrollment) do |f| %>
  <% if @class_group_enrollment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_group_enrollment.errors.count, "error") %> prohibited this class_group_enrollment from being saved:</h2>

      <ul>
      <% @class_group_enrollment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :class_group %><br />
    <%= f.text_field :class_group %>
  </div>
  <div class="field">
    <%= f.label :student %><br />
    <%= f.text_field :student %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
