= modal_for 'edit-template-modal' do
  = normal_modal_form_for [:admin, @template], resource: 'admin-template' do |f|
    = render 'form_fields', f: f
    = buttons_for @template, resource: 'admin-template'
