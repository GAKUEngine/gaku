<% @not_added_students.each {|enrollment| %>
  $('#course-students-index tbody').append('<%= render_js_partial("courses/enrollments/enrolled", {:enrollment => enrollment}) %>');
<% } %>
$('#new-course-class-group-link').show();

$('#new-course-enrollment-tab-link').html('<%= print_count(@course.students, t("course_enrollments.enrollments")) %>');
$('.course-students-count').html('<%= print_count(@course.students, t("students.list")) %>');

<% if flash.any? %>
	$('.flash-error').html("<%= escape_javascript(render 'shared/flash', :notice => flash[:notice]) %>");
<% else %>
  $(".flash-error").html("");
<% end %>