= remote_form_for [:admin, @school, @campus, @address], resource: 'admin-school-campus-address' do |f|
  = render 'form_fields', f: f
  = buttons_for @address, { resource: 'admin-school-campus-address', label: t(:'address.save') }
