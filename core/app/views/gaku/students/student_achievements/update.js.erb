$('#index-student-achievements-modal .modal-body .row-fluid .span12.well').html("<%= render_js_partial('index', {:student_achievements => @student_achievements}) %>");
$('.student-achievements-count').html('<%= print_count(@count, t_achievement_list) %>');

showNotice("<%= render_flash %>");
