// needed for student tab counter
$("#student-addresses-tab-link").html("<%= print_count(@count-1, t_address_plural) %>");

$(".<%= @resource_name.pluralize %>-count").html("<%= print_count(@count-1, t_address_list) %>");

showNotice("<%= render_flash %>");
console.log('<%=  @address.is_primary? %>')
<%- if @addressable.addresses.reload.any? & @primary_address %>
	$('#address-<%= @addressable.addresses.first.id %> td a').addClass('btn-primary');
<% end %>