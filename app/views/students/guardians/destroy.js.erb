$('#new_student_guardian_tab_link').html('<%= @student.guardians.count != 0 ? t('guardians.guardians') + "（" + @student.guardians.count.to_s + "）" : t('guardians.guardians') %>');
$('.guardians_count').html('<%= @student.addresses.count != 0 ? t('guardians.list') + "（" + @student.guardians.count.to_s + "）" : t('guardians.list') %>');
