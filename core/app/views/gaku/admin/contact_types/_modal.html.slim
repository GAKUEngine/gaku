= modal_for 'edit-contact-type-modal' do
  = modal_form_for [:admin, @contact_type], resource: 'admin-contact-type' do |f|
    = f.text_field :name, label: t(:'contact_type.name')
    = buttons_for @contact_type, resource: 'admin-contact-type'
