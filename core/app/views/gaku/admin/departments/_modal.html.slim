= modal_for 'edit-department-modal' do
  = modal_form_for [:admin, @department], resource: 'admin-department' do |f|
    = render 'form_fields', f: f
    = buttons_for @department, resource: 'admin-department'
