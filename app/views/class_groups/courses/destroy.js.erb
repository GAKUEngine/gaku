$('#class-group-courses-tab-link').html('<%= @class_group.courses.count != 0 ? t('courses.courses') + "（" + @class_group.courses.count.to_s + "）" : t('courses.courses') %>');
$('.courses-count').html('<%= @class_group.courses.count != 0 ? t('courses.list') + "（" + @class_group.courses.count.to_s + "）" : t('courses.list') %>');