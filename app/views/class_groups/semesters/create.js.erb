$('#semesters_index tbody').append('<%= escape_javascript(render :partial => "semester", :formats => [:html], :handlers => [:erb, :slim], :locals => {:semester => @semester}) %>');

$('.semesters_count').html('<%= @class_group.semesters.count %>');

$('#add_class_group_semester').html('');
$('#add_class_group_semester_link').show()

$('#class_group_semesters_tab_link').html('<%= @class_group.semesters.count != 0 ? t('semesters.semesters') + "（" + @class_group.semesters.count.to_s + "）" : t('semesters.semesters') %>');
$('.semesters_count').html('<%= @class_group.semesters.count != 0 ? t('semesters.list') + "（" + @class_group.semesters.count.to_s + "）" : t('semesters.list') %>');