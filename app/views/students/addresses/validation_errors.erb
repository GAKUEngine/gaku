<%- if params[:action] == 'create' %>

$('#new_student_address #validation_errors').html("<%= escape_javascript(render :partial => 'shared/errors', :formats => [:html], :handlers => [:erb, :slim], :locals => {:target => @address}) %>")

<%- elsif params[:action] == 'update' %>

$('#editAddressModal #validation_errors').html("<%= escape_javascript(render :partial => 'shared/errors', :formats => [:html], :handlers => [:erb, :slim], :locals => {:target => @address}) %>")

<% end %>