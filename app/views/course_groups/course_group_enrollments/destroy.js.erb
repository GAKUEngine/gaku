<%- if @course_group.course_group_enrollments.count == 0 %>
	$('#delete_course_group').html('<%= escape_javascript(render :partial => "/course_groups/delete_confirm", :formats => [:html], :handlers => [:erb, :slim]) %>');
<% end %>