<%-if @campus.contacts.any? %>
	$('#admin-school-campus-contacts-index').find('#contact-<%= @campus.contacts.first.id %> .make-primary').addClass('btn-primary');
<% end %>

showNotice("<%= render_flash %>");
$('.admin-school-campus-contacts-count').html('<%= print_count(@campus.contacts.count, t("contacts.list")) %>');
