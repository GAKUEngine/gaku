$('tr#contact_<%= @contact.id %>').html('<%= escape_javascript(render :partial => "student_contact", :formats => [:html], :handlers => [:erb, :slim], :locals => {:contact => @contact}) %>');
$(".modal#editContactModal").modal("hide");
