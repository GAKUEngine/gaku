= remote_form_for @course, url: enroll_class_group_course_enrollments_path(@course), method: :post, resource: 'course-class-group' do |f|
  = f.select :class_group_id, @class_groups, {prompt: t(:'class_group.choose')}, {class: 'chzn-select'}
  = buttons_for @course, { resource: 'course-class-group', label: t(:'class_group.enroll') }
