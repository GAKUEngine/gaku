showNotice("<%= render_flash %>");
$('.student-contacts-count').html("<%= print_count(@count-1, t_contact_list) %>");

<%- unless @student.contacts.blank? %>
	$('#student-contacts-index').find('#contact-<%= @student.contacts.first.id %> td.primary-button a').addClass('btn-primary');
	console.log($('#contact-<%= @student.contacts.first.id %> td.primary-button a'));
<% end %>

