$('#new-student-form').hide();
$('#students-index tbody').append('<%= escape_javascript(render :partial => "student", :formats => [:html], :handlers => [:erb, :slim], :locals => {:student => @student}) %>')
$('#new-student-form form')[0].reset();
