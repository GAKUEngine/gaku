$('#modal-dialogs').html("<%= escape_javascript(render :partial => 'student_modal', :formats => [:html], :handlers => [:erb, :slim], :locals => {:student => @student}) %>");
$('#edit-student-modal').modal('show');