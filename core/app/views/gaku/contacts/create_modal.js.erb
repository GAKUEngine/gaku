$('#new-contact-modal').modal('hide');
showNotice("<%= render_flash %>");

<% if @contactable.primary_contact.is_primary? %>
	$('td#primary-contact').html('<%= "#{@contactable.primary_contact.contact_type.name}: #{@contactable.primary_contact.data} (#{@contactable.primary_contact.details})" %>')
<% end %>