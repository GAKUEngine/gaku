= modal_for 'edit-enrollment-status-modal' do
  = modal_form_for [:admin, @enrollment_status], resource: 'admin-enrollment-status' do |f|
    = render 'form_fields', f: f
    = buttons_for @enrollment_status, resource: 'admin-enrollment-status'
