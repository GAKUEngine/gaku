$("#class-group-students table tbody").html('');
<% @class_group.class_group_enrollments.each {|enrollment| %>
  $('#class-group-students table tbody').append('<%= escape_javascript(render :partial => "student", :formats => [:html], :handlers => [:erb, :slim], :locals => {:enrollment => enrollment}) %>');
<% } %>

$('#class-group-students-tab a').html('<%=t("class_group_enrollments.list")+"（ "+@class_group.class_group_enrollments.count.to_s+" ）" %>');
$('.students-count').html('<%= t("class_group_enrollments.list")+"（ "+@class_group.class_group_enrollments.count.to_s+" ）" %>');
$('#student-modal').modal('hide');