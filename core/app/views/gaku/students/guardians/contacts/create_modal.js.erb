$('#new-contact-modal').modal('hide');
showNotice("<%= render_flash %>");

<% if @guardian.primary_contact.is_primary? %>
	$('td#primary-contact').html('<%= "#{@guardian.primary_contact.contact_type.name}: #{@guardian.primary_contact.data} (#{@guardian.primary_contact.details})" %>')
<% end %>