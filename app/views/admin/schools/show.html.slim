.row-fluid
  .span12.well
    table.table.table-bordered.table-striped
      tbody
        tr
          = render 'school_fields', :school => @school

= render 'admin/schools/campuses/campuses'

.row-fluid
  .span12
    =link_to t('Back'), @schools, :class => 'btn'