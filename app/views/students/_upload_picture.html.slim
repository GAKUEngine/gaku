= form_for @student, :url => student_path, :html => { :multipart => true } do |form|
  .row-fluid.mt-m
    = form.file_field :picture, :class => "form_file"
  .row-fluid.mt-xs
    = form.submit t('pictures.upload'), :class => "span12 btn btn-primary"
coffee:
  $(".form_file").attr('size', 9)
