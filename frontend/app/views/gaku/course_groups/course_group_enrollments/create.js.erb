$("#new-course-group-enrollment-link").show();
$("#new-course-group-enrollment").slide();
$('#course-group-enrollment-form-errors').html('');
$('#course-group-enrollments-index tbody').append('<%= j render("course", {enrollment: @course_group_enrollment}) %>');
$('.course-group-enrollments-count').html("<%= print_count(@count, t(:'course.list')) %>");

showNotice("<%= render_flash %>");
